## 1. Skill Foundation

- [x] 1.1 Create `.codex/skills/custom/ic_design/pynq-dev/` with `SKILL.md`,
  `STATE.md`, required rule references, and the state template; verify every
  required path exists.
- [x] 1.2 Define precise trigger conditions, OpenSpec and repository-rule hard
  gates, and specialized-skill routing in `SKILL.md`; verify the entry point
  links every required reference directly.

## 2. Development Contracts

- [x] 2.1 Add an affected-area workflow covering RTL/numeric, verification,
  export/runtime, Vivado, deployment, and board work; verify every area maps to
  required context and validation evidence.
- [x] 2.2 Add environment, filetree, and state rules with explicit generated
  artifact, credential, unavailable-tool, and incomplete-work behavior; verify
  the files reflect `AGENTS.md` and `docs/rules/filetree.md` constraints.

## 3. Validation and Handoff

- [x] 3.1 Run generic skill validation and a local structural/content audit;
  verify metadata, required files, internal links, OpenSpec gates, and state
  statuses pass.
- [x] 3.2 Run strict OpenSpec validation, verify repository path-rule
  conformance, and update both `STATE.md` and this checklist with concrete
  evidence.
