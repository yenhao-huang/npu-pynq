## 1. Roadmap Update

- [x] 1.1 Replace only the empty product-milestone statement in
  `docs/human/roadmap.md` with the eight human-confirmed Phase 0–3 bullets and
  verify their identifiers, order, and wording.

## 2. Validation

- [x] 2.1 Read the roadmap back, verify no unapproved human document changed,
  run strict OpenSpec validation and `git diff --check`, and record the evidence
  in `pynq-dev/STATE.md`.

## 3. Priority Ordering

- [x] 3.1 Move the complete `Planned milestones` section before
  `Confirmed direction` without changing either section's content, then read the
  roadmap back and verify heading order, phase order, strict OpenSpec validity,
  and `git diff --check`.
