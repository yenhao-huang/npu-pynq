## Why

The human-owned roadmap currently contains no product milestones. The team has
now confirmed the high-level development phases from numeric contracts through
ResNet-18 production hardening and a Small Transformer feasibility study.

## What Changes

- Replace the empty product-milestone statement in `docs/human/roadmap.md` with
  the confirmed Phase 0, Phase 1A–1C, Phase 2A–2C, and Phase 3 direction.
- Keep the roadmap intentionally high-level: one concise outcome per phase.
- Place `Planned milestones` before `Confirmed direction` so the most important
  product direction appears first.
- Do not add dates, detailed architecture, implementation tasks, performance
  commitments, feature-list entries, or changelog entries.

## Capabilities

### New Capabilities

None. This is a human-confirmed documentation-only update.

### Modified Capabilities

None.

## Impact

- Modifies only `docs/human/roadmap.md` outside this OpenSpec change.
- Does not change product behavior, APIs, RTL, tests, runtime, exporter,
  bitstreams, board state, feature inventory, or release history.
