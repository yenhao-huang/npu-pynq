# State rule

For a new independent run, copy `references/template/STATE.template.md` to `STATE.md`, fill in Run ID, Started, and Scope, then mark the active step `in_progress`. Record concrete evidence before marking any step complete.
