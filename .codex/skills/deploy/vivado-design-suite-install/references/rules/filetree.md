# Filetree rule

Keep this project-local skill at:

```text
npu_repo_in_pynq/.codex/skills/deploy/vivado-design-suite-install/
```

Required files are `SKILL.md`, `STATE.md`, `references/rules/{env,filetree,state-rules}.md`, and `references/template/STATE.template.md`. Keep domain documentation directly under `references/`.
